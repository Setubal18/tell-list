import { TranslateCategoriaPipe } from './translate-categoria.pipe';

describe('TranslateCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
